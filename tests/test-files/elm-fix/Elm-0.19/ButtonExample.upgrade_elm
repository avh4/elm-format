module Main exposing (upgrade_Button_buttonDeprecated)

import ElmFix
import Nri.Ui.Button.V9 as Button


upgrade_Nri_DEPRECATED_Button_buttonDeprecated ({ size, width, onClick, icon, disabled, label, style } as config) =
    Button.button label
        [ case size of
            Button.Large ->
                Button.large
        , case width of
            Just w ->
                Button.exactWidth w

            Nothing ->
                ElmFix.remove
        , Button.onClick onClick
        , case style of
            Button.Premium ->
                Button.premium

            Button.Normal ->
                ElmFix.remove
        , case icon of
            Nothing ->
                ElmFix.remove

            Just i ->
                Button.icon i
        , if disabled then
            Button.disabled

          else
            ElmFix.remove
        ]
