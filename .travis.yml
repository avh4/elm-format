sudo: false


language: haskell
cabal: "3.2"
ghc: "8.8.4"


cache:
  directories:
  - "$HOME/.cabal/store"


install:
- cabal v2-update
- cabal v2-build --only-dependencies
- cabal v2-build --only-dependencies --enable-tests
- ./build.sh -- dependencies


script:
- ./build.sh
